# Auto generated by MySQL Workbench Schema Exporter.
# Version 2.1.5-dev (doctrine2-yaml) on 2014-11-23 13:07:07.
# Goto https://github.com/johmue/mysql-workbench-schema-exporter for more
# information.

Entity\OauthSession:
    type: entity
    table: oauth_sessions
    indexes:
        client_id:
            columns: [ client_id ]
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    fields:
        owner_type:
            type: string
            length: 255
        owner_id:
            type: string
            length: 255
        client_id:
            type: string
            length: 255
        client_redirect_uri:
            type: string
            length: 255
            nullable: true
    oneToMany:
        oauthAccessTokens:
            targetEntity: OauthAccessToken
            mappedBy: oauthSession
            joinColumn:
                name: id
                referencedColumnName: session_id
                onDelete: CASCADE
        oauthAuthCodes:
            targetEntity: OauthAuthCode
            mappedBy: oauthSession
            joinColumn:
                name: id
                referencedColumnName: session_id
                onDelete: CASCADE
        oauthSessionScopes:
            targetEntity: OauthSessionScope
            mappedBy: oauthSession
            joinColumn:
                name: id
                referencedColumnName: session_id
                onDelete: CASCADE
    manyToOne:
        oauthClient:
            targetEntity: OauthClient
            inversedBy: oauthSessions
            joinColumn:
                name: client_id
                referencedColumnName: id
                onDelete: CASCADE