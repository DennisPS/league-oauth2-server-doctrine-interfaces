# Auto generated by MySQL Workbench Schema Exporter.
# Version 2.1.5-dev (doctrine2-yaml) on 2014-11-23 13:07:07.
# Goto https://github.com/johmue/mysql-workbench-schema-exporter for more
# information.

Entity\OauthAuthCode:
    type: entity
    table: oauth_auth_codes
    indexes:
        session_id:
            columns: [ session_id ]
    id:
        auth_code:
            type: string
            length: 255
    fields:
        session_id:
            type: integer
        expire_time:
            type: integer
        client_redirect_uri:
            type: string
            length: 255
    oneToMany:
        oauthAuthCodeScopes:
            targetEntity: OauthAuthCodeScope
            mappedBy: oauthAuthCode
            joinColumn:
                name: auth_code
                referencedColumnName: auth_code
                onDelete: CASCADE
    manyToOne:
        oauthSession:
            targetEntity: OauthSession
            inversedBy: oauthAuthCodes
            joinColumn:
                name: session_id
                referencedColumnName: id
                onDelete: CASCADE