# Auto generated by MySQL Workbench Schema Exporter.
# Version 2.1.5-dev (doctrine2-yaml) on 2014-11-23 13:07:07.
# Goto https://github.com/johmue/mysql-workbench-schema-exporter for more
# information.

Entity\OauthAccessTokenScope:
    type: entity
    table: oauth_access_token_scopes
    indexes:
        access_token:
            columns: [ access_token ]
        scope:
            columns: [ scope ]
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    fields:
        access_token:
            type: string
            length: 255
        scope:
            type: string
            length: 255
    manyToOne:
        oauthAccessToken:
            targetEntity: OauthAccessToken
            inversedBy: oauthAccessTokenScopes
            joinColumn:
                name: access_token
                referencedColumnName: access_token
                onDelete: CASCADE
        oauthScope:
            targetEntity: OauthScope
            inversedBy: oauthAccessTokenScopes
            joinColumn:
                name: scope
                referencedColumnName: id
                onDelete: CASCADE