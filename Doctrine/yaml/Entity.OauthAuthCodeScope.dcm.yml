# Auto generated by MySQL Workbench Schema Exporter.
# Version 2.1.5-dev (doctrine2-yaml) on 2014-11-23 13:07:07.
# Goto https://github.com/johmue/mysql-workbench-schema-exporter for more
# information.

Entity\OauthAuthCodeScope:
    type: entity
    table: oauth_auth_code_scopes
    indexes:
        auth_code:
            columns: [ auth_code ]
        scope:
            columns: [ scope ]
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    fields:
        auth_code:
            type: string
            length: 255
        scope:
            type: string
            length: 255
    manyToOne:
        oauthAuthCode:
            targetEntity: OauthAuthCode
            inversedBy: oauthAuthCodeScopes
            joinColumn:
                name: auth_code
                referencedColumnName: auth_code
                onDelete: CASCADE
        oauthScope:
            targetEntity: OauthScope
            inversedBy: oauthAuthCodeScopes
            joinColumn:
                name: scope
                referencedColumnName: id
                onDelete: CASCADE