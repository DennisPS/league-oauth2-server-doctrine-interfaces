# Auto generated by MySQL Workbench Schema Exporter.
# Version 2.1.5-dev (doctrine2-yaml) on 2014-11-23 13:07:07.
# Goto https://github.com/johmue/mysql-workbench-schema-exporter for more
# information.

Entity\OauthSessionScope:
    type: entity
    table: oauth_session_scopes
    indexes:
        session_id:
            columns: [ session_id ]
        scope:
            columns: [ scope ]
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    fields:
        session_id:
            type: integer
        scope:
            type: string
            length: 255
    manyToOne:
        oauthSession:
            targetEntity: OauthSession
            inversedBy: oauthSessionScopes
            joinColumn:
                name: session_id
                referencedColumnName: id
                onDelete: CASCADE
        oauthScope:
            targetEntity: OauthScope
            inversedBy: oauthSessionScopes
            joinColumn:
                name: scope
                referencedColumnName: id
                onDelete: CASCADE